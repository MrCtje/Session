import { RequestFocusDirective } from './request-focus.directive';

describe('RequestFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new RequestFocusDirective();
    expect(directive).toBeTruthy();
  });
});
